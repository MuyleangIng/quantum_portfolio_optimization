# Quantum Portfolio Optimization

This project uses FastAPI and Qiskit to perform quantum portfolio optimization.

## Setup

1. Run `source venv/bin/activate` to activate the virtual environment.
2. Run `uvicorn app.main:app --reload` to start the FastAPI server.
3. Access the API documentation at http://localhost:8000/docs
